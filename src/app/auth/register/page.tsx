import RegisterForm from "./RegisterForm";

export const metadata = {
  title: "Register - Zalient",
  description: "Register to your account",
};

export default function LoginPage() {
  return <RegisterForm />;
}
