import { ReactNode } from "react";

export const metadata = {
  title: "Zalient - NFC Hub",
  description: "NFC Hub",
};
export default function LandingLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
